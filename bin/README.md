This folder contains a few files that can be run with Node.

For example, to print information about the current day to the console, do `node day` from this directory. Pass `-h` or `--help` to any file to get info on how to use it.